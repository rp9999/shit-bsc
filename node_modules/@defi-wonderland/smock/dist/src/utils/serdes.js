"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isPojo = exports.isStruct = exports.getObjectAndStruct = exports.convertStructToPojo = void 0;
const proto = Object.prototype;
const gpo = Object.getPrototypeOf;
function convertStructToPojo(struct) {
    let obj = {};
    Object.entries(struct)
        .slice(struct.length)
        .forEach(([key, value]) => {
        obj[key] = isStruct(value) ? convertStructToPojo(value) : value;
    });
    return obj;
}
exports.convertStructToPojo = convertStructToPojo;
function getObjectAndStruct(obj1, obj2) {
    if (isPojo(obj1) && isStruct(obj2)) {
        return [obj1, obj2];
    }
    if (isPojo(obj2) && isStruct(obj1)) {
        return [obj2, obj1];
    }
    return;
}
exports.getObjectAndStruct = getObjectAndStruct;
function isStruct(obj) {
    return Array.isArray(obj) && Object.keys(obj).length > obj.length;
}
exports.isStruct = isStruct;
function isPojo(obj) {
    if (obj === null || typeof obj !== 'object') {
        return false;
    }
    return gpo(obj) === proto;
}
exports.isPojo = isPojo;
//# sourceMappingURL=serdes.js.map